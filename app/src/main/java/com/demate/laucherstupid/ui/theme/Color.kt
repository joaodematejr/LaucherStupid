package com.demate.laucherstupid.ui.theme

import androidx.compose.ui.graphics.Color

// ========== CORES PRINCIPAIS ==========
// Azul profissional - cor primária para confiança e tecnologia
val PrimaryBlue = Color(0xFF1E3A8A)        // Azul escuro profissional
val PrimaryBlueLight = Color(0xFF3B82F6)   // Azul claro vibrante
val PrimaryBluePale = Color(0xFFDEEAFF)    // Azul muito claro para containers

// Verde para sucesso e transações aprovadas
val SuccessGreen = Color(0xFF059669)       // Verde escuro
val SuccessGreenLight = Color(0xFF10B981)  // Verde claro
val SuccessGreenPale = Color(0xFFD1FAE5)   // Verde muito claro

// Laranja para alertas e ações importantes
val AccentOrange = Color(0xFFEA580C)       // Laranja escuro
val AccentOrangeLight = Color(0xFFF97316)  // Laranja claro
val AccentOrangePale = Color(0xFFFED7AA)   // Laranja muito claro

// ========== CORES NEUTRAS ==========
// Cinzas para textos e backgrounds
val NeutralGray900 = Color(0xFF111827)     // Quase preto
val NeutralGray800 = Color(0xFF1F2937)     // Cinza muito escuro
val NeutralGray700 = Color(0xFF374151)     // Cinza escuro
val NeutralGray600 = Color(0xFF4B5563)     // Cinza médio
val NeutralGray500 = Color(0xFF6B7280)     // Cinza
val NeutralGray400 = Color(0xFF9CA3AF)     // Cinza claro
val NeutralGray300 = Color(0xFFD1D5DB)     // Cinza muito claro
val NeutralGray200 = Color(0xFFE5E7EB)     // Cinza quase branco
val NeutralGray100 = Color(0xFFF3F4F6)     // Cinza super claro
val NeutralGray50 = Color(0xFFF9FAFB)      // Quase branco

// ========== CORES DE STATUS ==========
// Vermelho para erros e falhas
val ErrorRed = Color(0xFFDC2626)           // Vermelho erro
val ErrorRedLight = Color(0xFFEF4444)      // Vermelho claro
val ErrorRedPale = Color(0xFFFEE2E2)       // Vermelho muito claro

// Amarelo para avisos
val WarningYellow = Color(0xFFD97706)      // Amarelo escuro
val WarningYellowLight = Color(0xFFF59E0B) // Amarelo claro
val WarningYellowPale = Color(0xFFFEF3C7)  // Amarelo muito claro

// Azul ciano para informações
val InfoCyan = Color(0xFF0891B2)           // Ciano escuro
val InfoCyanLight = Color(0xFF06B6D4)      // Ciano claro
val InfoCyanPale = Color(0xFFCFFAFE)       // Ciano muito claro

// ========== BACKGROUNDS ESPECIAIS ==========
// Para telas escuras
val DarkBackground = Color(0xFF0F172A)     // Fundo escuro principal
val DarkSurface = Color(0xFF1E293B)        // Superfície escura
val DarkCard = Color(0xFF334155)           // Cards escuros

// Para telas claras
val LightBackground = Color(0xFFFFFFFF)    // Branco puro
val LightSurface = Color(0xFFFAFAFA)       // Cinza super claro
val LightCard = Color(0xFFFFFFFF)          // Branco para cards

// ========== CORES PARA FUNCIONALIDADES ESPECÍFICAS ==========
// Sistema e configurações
val SystemBlue = Color(0xFF2563EB)         // Azul sistema
val SystemBluePale = Color(0xFFEBF4FF)     // Azul sistema claro

// Comunicação (WiFi, Bluetooth, etc.)
val CommunicationPurple = Color(0xFF7C3AED) // Roxo comunicação
val CommunicationPurplePale = Color(0xFFF3E8FF) // Roxo claro

// Hardware e especificações
val HardwareGray = Color(0xFF64748B)       // Cinza hardware
val HardwareGrayPale = Color(0xFFF1F5F9)   // Cinza hardware claro

// Bateria e energia
val BatteryGreen = Color(0xFF16A34A)       // Verde bateria
val BatteryGreenPale = Color(0xFFDCFCE7)   // Verde bateria claro

// ========== COMPATIBILIDADE COM TEMA ANTIGO ==========
// Manter compatibilidade enquanto migra
val Purple80 = PrimaryBluePale
val PurpleGrey80 = NeutralGray300
val Pink80 = AccentOrangePale

val Purple40 = PrimaryBlue
val PurpleGrey40 = NeutralGray600
val Pink40 = AccentOrange
